$(document).ready(function() {

let new_task = $("<%= j (render '/, task: @task) %>");
// Je crée ma variable new_gossip. Le j signifie escape javascript et permet d'executer de l'erb.

$('ul').append(new_task);

})
$("#categories").find("ul").hide()
});
